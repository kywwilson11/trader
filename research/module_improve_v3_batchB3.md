# Module improvement campaign — panel review (v3) — Batch B3: Cost Model (fees / liquidity / cost regime)

## Verdict

Batch B3 covered the three modules that make up the cost model — `liquidity`, `cost_regime`, and `fees` — each reviewed independently by 5 Opus reviewers against an identical brief, adjudicated into a single hardening spec, implemented by Sonnet, and hardened by Fable. Combined: 190 raw findings (58 / 70 / 62) spanning the EDGE bid-ask spread estimator, the VIX/Amihud cost-regime features, and the fee/spread/slippage cost ladder shared by the backtester and every live gate. All three landed as **approved-after-hardening**: everything shipped is instrumentation (suspicious-stamp / degenerate-spread / coverage warnings), fail-open/fail-closed parity fixes, input validation, docstring-precision corrections, a per-process FRED memo, and 4 confirmed bugfixes on latent edge cases (two separate +inf mis-attributions in warning logic, one impossible-clamp docstring, one missed +inf spread guard) plus 2 perf micro-optimizations and 1 modernization in `liquidity` — zero changes to feature values, thresholds, or trading semantics shipped from this pipeline. No modules were dropped for budget. The regression gate (`bash scripts/ab_check.sh`) passed clean: zero NEW failure names and zero DISAPPEARED names against `tests/baseline_failures.txt`, exit code 0. 26 findings were deferred to the owner as model-facing or policy-semantic decisions, headlined by three separate 5/5-consensus items: fees' "unified edge-floor ruling" (four bindings spanning `execution_policy`, `backtest`, and `meta_label`), liquidity's no-estimate-fill floor discount plus its never-stamped crypto EDGE column, and cost_regime's `pct_change` fill_method machine-dependence (silent Mac-vs-Jetson value drift).

## Per-module results

| Module | Raw findings | Accepted | Rejected | Verdict | Bugs fixed / Perf / Modernized |
|---|---|---|---|---|---|
| `liquidity` | 58 | 12 | 8 | approved-after-hardening | 1 / 2 / 1 |
| `cost_regime` | 70 | 15 | 6 | approved-after-hardening | 1 / 0 / 0 |
| `fees` | 62 | 8 | 8 | approved-after-hardening | 2 / 0 / 0 |

## Shipped

- fix(liquidity): EDGE estimator-health instrumentation + impact fail-open parity, input validation, bidask declared, v3 test pins
- fix: cost_regime — PIT VIX alignment hardening (tz symmetry, one-obs/day lag, index validation, float64 pinning), FRED parse/fetch contract + per-process memo, Amihud guards; 31 tests
- feat: fees v3 — journal-scan row guards + prefilter, call-time constant defaults, degenerate-spread warnings (negative/NaN/inf), cost-ladder docs, 29-test contract suite

## OWNER DECISIONS (deferred — model-facing or policy-semantic)

Every line below is verbatim from the panel's deferred_to_owner output, grouped by module in panel order; these are decisions only the owner can make, not shipped fixes.

### liquidity (8 items)

- FLOOR-VS-FLAT NO-ESTIMATE FILL (consensus 5, model-facing): every bar EDGE cannot estimate (halts, zero-range windows, NaN bars, warmup, n<window) is stamped SPREAD_FLOOR_PCT=0.02 — measured 26% cheaper round trip (0.083% vs 0.113% stock; 0.52% vs 0.60% crypto) than the flat fallback the same module charges when the stamp is absent, on exactly the least-liquid bars; flips meta_label's `net > 0` binary labels in the 0.03pp band. Fix sketch: add `na_fill_pct=None` to edge_spread_series (None -> today's floor_pct, zero-change default); harvest passes fees.FLAT_SPREAD_PCT['stock']; keep never-NaN (harvest dropna depends on it). Jetson re-harvest + promotion gate. Decide TOGETHER with the hourly-EDGE-level item below — raising the fill while the level is inflated compounds the error. The shipped instrumentation sizes the blast radius first.
- CRYPTO BOOK NEVER STAMPED (consensus 5, model-facing): scripts/harvest_stock_data.py:120 is the repo's only Eff_Spread_Pct stamp site; crypto (0.50% fee load, widest per-pair spread dispersion) still runs the flat 0.10% haircut the module was built to replace, silently (the `if 'Eff_Spread_Pct' in columns` guard never fires, so no log exists). Fix sketch: stamp in harvest_crypto_data.py with a crypto-appropriate window (35 hourly bars = 1.5 days on 24/7; ~120 bars matches the 5-trading-day intent). Gated behind the EDGE-level question; kill-list-adjacent (wave-7 entry) so explicit owner call required. Docstring scope note ships now.
- HOURLY EDGE LEVEL / WAVE-7 KILL-RATIONALE UNIT ERROR (consensus 3, evidence only — no code proposed): Reviewers 3 and 4 independently reproduced the shipped estimator on 60d/1h yfinance bars at NVDA ~0.30 PERCENT (30 bps) median — numerically the wave-7 refutation's '0.3' with the unit label flipped from percent to bps, and 25.3/0.3=84x sits inside the quoted '~70-95x' discrepancy, suggesting research/KILL_LIST.md:90's stated reason rests on a percent/bps slip. Reviewer 4's DGP control shows the estimator itself is accurate, so this is a bar-frequency/data question, not a code bug. 30-second Mac reproduction: python3 -c "import yfinance as yf, liquidity as L; d=yf.download('NVDA',period='60d',interval='1h',progress=False,auto_adjust=False); print(L.edge_spread_series(d.droplevel(1,axis=1)).median())" — read the result as PERCENT. The shipped harvest [SPREAD] summary settles it from production Alpaca bars. KILL_LIST entries leave only by owner decision; nothing killed is being rebuilt.
- JETSON BIDASK PRESENCE CHECK (consensus 2, sequencing): before pip-installing from the updated requirements on the Jetson, run `python -c 'import bidask'` there. If ABSENT, every Eff_Spread_Pct in the current training parquet came from the several-fold upward-biased AR fallback, and installing bidask changes stamped costs on the next harvest — treat as model-facing (re-harvest + promotion gate), not a silent pip install.
- INF-TO-FLOOR ON THE FALLBACK PATH (consensus 1, model-facing): `replace(inf->nan)` runs BEFORE clip, so a +inf AR-fallback estimate (one Low<=0 print) is stamped at the 0.02 FLOOR instead of the 1.50 cap — the bad print poisons costs in the cheap direction for a full window. One-line reorder sketch (clip before replace); changes stamped values on affected bars only.
- ENTRY-BAR SPREAD PAYS BOTH CROSSINGS (consensus 1, model-facing): backtest.py:333/meta_label.py:420 index the round-trip cost at entry bar i while the exit lands at bar j; stressed exits (hard stops) land where spreads are widest, so losing trades are charged pre-stress spreads. Sketch: sibling helper `round_trip_cost_entry_exit(asset, s, entry_idx, exit_idx)` = fee_const + 0.5*s[i] + 0.5*s[j], leaving per_bar_round_trip_cost byte-identical until callers opt in; measure s[j]-s[i] distribution by exit reason first. Contract note ships in the docstring now.
- ZERO-SPREAD-AS-MISSING (consensus 1, model-facing): changing `(s >= 0.0)` to `(s > 0.0)` in per_bar_round_trip_cost is provably a no-op on producer data (floored at 0.02) but changes charged costs on any externally-supplied zero column; the corruption WARNING ships now.
- CONSUMER-SIDE CAP CLIP (consensus 2, model-facing): clipping per_bar_round_trip_cost input at SPREAD_CAP_PCT would change charged costs for out-of-band data; the WARNING ships now.

### cost_regime (10 items)

- pct_change fill_method pin (consensus 5, model-facing): amihud_illiq line 50 inherits a version-dependent default — pandas 2.x (Jetson) pads NaN closes, pandas 3.x (this Mac) does not, so the same OHLCV yields different Amihud values per machine. Sketch: write c.pct_change(fill_method=None) (PIT-honest, literal no-op on pandas 3, valid on 2.0-3.x) OR c.ffill().pct_change(fill_method=None) to preserve Jetson values; decide NOW while zero callers/artifacts exist, and consider a repo-wide sweep of the ~30 other unpinned pct_change sites plus a pandas upper bound in requirements-jetson.txt.
- VIX_Regime NaN->1 fail-open in the array path (consensus 4, model-facing): the lagged NaN on the first VIX observation day maps to a fabricated 'normal' (1) while VIX_Level/Pctile are NaN — unknown resolves to the benign middle bucket and the learner cannot distinguish it. Sketch: regime = lagged.map(vix_regime_code, na_action='ignore') so VIX_Regime is NaN exactly where VIX_Level is NaN; keep the scalar vix_regime_code(nan)->1 contract for live callers. Decide together with the neutral-fill question.
- 3-tier VIX_Regime vs the LIVE 4-tier macro_indicators ladder (consensus 2, model-facing): the crisis/halt tier (>35) the live sizer acts on hardest is invisible to the feature (VIX 26 == VIX 60 == 2). Sketch: (a) document the divergence as deliberate, (b) add tier 3 at 35.0 (strict refinement, nests), or (c) drop fixed buckets and rely on VIX_Pctile; settle before wave-8 wiring.
- Weekend/holiday VIX freshness (consensus 2, model-facing): shift(1)+ffill makes ~29.5% of 24/7 crypto bars one full trading day staler than PIT-legal (Saturday sees Thursday, not Friday). Sketch: as-of strictly-before-the-bar-day join (short_cost.pit_publication_map's searchsorted(side='left')-1 idiom), gated on bar tz at/west of ~UTC+2:45; requires harvest re-run + retrain + challenger->shadow->DM-HLN.
- macro_indicators.py FRED fallback duplication (consensus 2, policy-semantics, edit lands OUTSIDE this module): the live sizing path hand-rolls the same fredgraph.csv URL with no context manager and a len(parts)==2 parser that breaks on any 3-column body -> 'VIX unavailable' -> all ladders pass at 1.0x. Sketch: delegate to cost_regime.parse_fred_vixcls(body).iloc[-1]; changes LIVE sizing failure behavior, so not a drive-by refactor.
- Amihud stationarity companion (consensus 1 but cites the wave-8 ledger's own intent, model-facing): the wave-8 item wants a per-name illiquidity RANK; the module emits a raw level spanning ~3 decades across names (behaves as a name identifier in a pooled panel). Sketch: add Amihud to panel_ranks.CS_RANK_BASE_COLS at wiring time (machinery exists) or emit an Amihud_Z companion mirroring SVR_21/SVR_Z.
- Amihud window default per book (consensus 2, model-facing): default 21 is Amihud's DAILY month applied to an HOURLY grid — ~0.9 calendar days crypto vs ~3.2 sessions stocks from one nominal parameter. Docstring now states the unit; choosing per-book bar counts (e.g. 24*21 crypto / 7*21 stocks) is a wiring-time value decision.
- Neutral-fill policy for the three VIX columns before harvest dropna (consensus 1, model-facing): harvest scripts dropna() next; R5 measured 43% of rows deleted on a short VIX slice. The coverage warning ships now; picking fill values (e.g. 0.5 Pctile / 1 Regime, mirroring harvest_crypto_data._fill_archive_features) changes feature values and must pair with the wiring/retrain.
- Optional by= group key for panel-safe amihud_illiq (consensus 1, API surface): per-group pct_change/rolling with default None == today's path. The non-unique-index warning ships now; the new API and its cross-name semantics belong to the wiring decision.
- On-disk VIXCLS cache with stale-data fallback and/or retry (consensus 2, changes failure-path semantics): would return dated data where today returns None, and an append-style cache is exactly what creates the duplicate-date hazard (now defused by the collapse, but still). Per-process memo ships now; persistence policy is the owner's.

### fees (8 items)

- THE UNIFIED EDGE-FLOOR RULING (consensus 5, the panel's converged top item): one decision, four bindings. (a) Ratify which quantity is 'the edge floor' — the code already binds required_edge_pct (2.0x raw) in BOTH admitting gates (backtest.py:277 `edge_floor`, order_utils.py:636 live), under which EXEC_EDGE_HEADROOM_MULT=1.5 means 3.0x raw and is non-vacuous; if raw cost is preferred instead, EXEC_EDGE_HEADROOM_MULT must rise above 2.0 or the post-headroom branch is dead. Ratifying also resolves execution_policy.py's UNPINNED CONTRACT block and lets TestEdgeFloorContractFork assert one reading. (b) Spread axis: backtest admits on the FLAT 0.10/0.05 while charging per-bar Eff_Spread_Pct to net P&L and live admits on the real quote — verified neither-superset-nor-subset (BTC at a 2bp spread: live floor 0.84-1.04 vs certified 1.20; a 0.40%-spread name: live 0.93 vs certified 0.226). Either move the backtest floor to per-bar or record the flat floor as accepted. (c) meta_label._gen_meta_rows applies NO cost floor and admits at threshold*0.5 = 0.075% at the shipped default vs the 1.20% crypto deployment floor (16x; still 2.4x at the top of the search range) — add required_edge_pct to its admission (meta retrain) or record the relaxation with the multiple stated. (d) NEW quantified fact from this panel: the default Optuna trade_threshold search range [0.05, 1.00] (adaptive_config.py:29, hypersearch_v2.py:621) sits ENTIRELY below the 1.20% crypto floor while the objective charges 1.0x cost with no floor — trade_threshold is inert for crypto and models are selected on a population the deployed policy cannot trade; fixing the objective is a scoring change -> CLAUDE.md gotcha #2 (delete v2_study.db/stock_v2_study.db, reset adaptive best_score).
- Degenerate-spread VALUE policy: whether the scalar round_trip_cost_pct should sanitize negative/non-finite spread_pct to FLAT_SPREAD_PCT[asset_type] like its vectorized twin (liquidity.py:185) — today a crossed quote prices the live floor at fee-only (verified 0.50 vs 0.60 crypto) and a NaN floor fails OPEN in backtest.py:315. Sanitizing changes live admission on crossed quotes AND flips backtest from fail-open to priced; rule it together with (b) above. The warning shipping now makes occurrences countable first.
- MAKER_ENTRIES_ENABLED staleness: crypto_entry_fee_bps(live=True) never reads the flag, so flipping it off leaves up to 14 days of maker-heavy journals under-pricing the live crypto entry fee by up to 10 bps (floor 1.00 vs 1.20). Cheapest fix: short-circuit to CRYPTO_TAKER_BPS when strategy_config.MAKER_ENTRIES_ENABLED is False (try/except import -> taker on failure); alternatives: a recency sub-window (e.g. >=5 entries in 48h) or age-weighting. All change the live floor — tightening-only, but still admission semantics.
- Notional-weighted maker share: the blend treats an ORDER-count share as a per-dollar fee weight; place_maker_buy labels a 96%-passive fill 'taker_fallback' (verified, order_utils.py:294/313-319), biasing the share DOWN. base_loop already journals final_notional, so a dollar-weighted share is computable — it can only LOOSEN the live gate, so it needs the challenger->shadow->DM-HLN path plus a Jetson-journal measurement of the delta first.
- STOCK_SLIPPAGE_BPS_PER_SIDE double-count question + the crypto/stock asymmetry: stock pays 6 bps/RT of crossing allowance ON TOP of a full spread term the docstring says already prices both crossings (11.3 bps flat vs 5 bps of spread), while crypto — whose taker-fallback exits are the most slippage-exposed — carries no allowance at all. Ship NOTHING; first run the measurement the repo already records: compare journaled per-side |slippage_bps| against the 3.0 bps allowance over >=60 days of Jetson journals (kill-list-safe: a printed comparison, not realized-shortfall feedback into offline cost). Any constant change is gotcha #2 and must move hypersearch's TXN_COST_PCT in lockstep.
- hypersearch_v2 importing fees (or correcting TXN_COST_PCT['stock'] 0.11 -> 0.113): a training-objective change -> gotcha #2. The new source-pin test freezes today's values with the 0.003 rounding explicitly tolerated until this is ruled.
- Per-file I/O tolerance in the maker-share scan: today one unreadable/mid-rotation file (TOCTOU between exists() and open()) aborts all 15 days -> None -> full taker for the TTL. Keeping that is fail-closed and now documented as deliberate; letting readable days produce a share in a degraded state loosens the live gate under I/O failure and is the owner's call (option: count file errors and still force None, which is outcome-identical but better logged).
- execution_report.py follow-ups (cross-module instrumentation, its output text is byte-pinned by tests/test_review_b17.py — separate pass): (i) its maker share is computed over rows pre-filtered to slippage_bps is not None (execution_report.py:49) while the live gate counts every crypto buy — the in-source 'same share the live fee gate computes' comment is false for rows with None slippage; (ii) the closing guidance compares a PER-FILL mean shortfall against ROUND-TRIP spread haircuts — off by 2x in the direction that hides an optimistic backtest; (iii) run_report's days=14 default is a literal, not fees.MAKER_SHARE_WINDOW_DAYS.

## Verification

Gate: **PASS**. Ran `bash scripts/ab_check.sh` in `/Users/kywwilson/Desktop/Projects/trader` (read-only, no edits made, baseline not regenerated). Verdict verbatim: "ab_check: suite summary: ====== 21 failed, 2583 passed, 18 skipped, 3 warnings, 7 errors in 15.31s ======" / "NEW failures: none" / "DISAPPEARED failures: none" / "ab_check: PASS — zero regressions vs tests/baseline_failures.txt". Exit code confirmed 0. The FAILED/ERROR set is byte-identical to the committed `tests/baseline_failures.txt` — 21 FAILED + 7 ERROR, all pre-existing missing-dependency cases (dotenv/torch/lightgbm/optuna/joblib/numba/sklearn), e.g. test_model_v2, test_predict_now, test_hypersearch_v2, test_new_modules GARCH/HMM tests, test_oi_archive, test_wave4 ShortFlow, test_shadow promote tests, test_imports crypto_loop/stock_loop/predict_now, test_data_utils parquet tests, test_asof_universe, test_conviction_journal, test_panel_ranks BouncedLoser — none of which touch `liquidity.py`, `cost_regime.py`, or `fees.py`. Passed count (2583) and skipped count (18) are higher than CLAUDE.md's documented 2026-07-15 snapshot (1887/15) purely from tests added since then by the other uncommitted campaigns; ab_check.sh (and this report) judge by failure-NAME diff only, per the skill's explicit "counts are advisory" rule, so that drift is not a regression signal. Since there are zero NEW failure names, nothing in the current diff is attributable to `liquidity`, `cost_regime`, or `fees` — the gate criterion (no NEW names) is satisfied cleanly.

Nothing in this batch has been committed — all diffs remain in the working tree pending owner review and commit. As with every campaign on this dev Mac, verification is bounded by the two-machine split: `liquidity.py`, `cost_regime.py`, and `fees.py` import none of the Jetson-only heavy deps (torch/lightgbm/optuna/joblib/numba/sklearn/dotenv), so this gate run exercises them in full on this machine — but the 21 FAILED + 7 ERROR baseline itself IS exactly those Jetson-only modules this Mac cannot import, so full-suite green is confirmed only on the Jetson stack.

Generated by module-improve-v3 (Opus panel -> Fable spec -> Sonnet implement -> Fable harden).
