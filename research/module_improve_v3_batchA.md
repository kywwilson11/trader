# Panel review — batch A: live trading engine & order path

## Verdict

Five modules — `base_loop`, `order_utils`, `execution_policy`, `risk_budget`, `bet_sizing` — each went through five independent Opus reviewers on an identical brief, adjudicated into a single spec by Fable, implemented by Sonnet, and hardened by a second Fable pass (the module-improve-v3 panel pipeline). Combined across the five modules: 384 raw findings, 67 accepted and shipped, 36 rejected on Fable's anchor re-verification, for a code delta of +1192/-279 across the five modules plus five new `tests/test_*_v3.py` files. All five modules reached an approved verdict (`bet_sizing` approved clean; the other four approved-after-hardening). **The workflow's own `[gate]` and `[report]` agents FAILED** — both hit "You've hit your session limit · resets 10pm (America/Los_Angeles)" before the regression gate could run and before this report could be generated by the workflow itself, leaving the campaign's own record incomplete. Because of that failure, the regression gate was re-run manually afterward, with this verbatim result: "ab_check: PASS — zero regressions vs tests/baseline_failures.txt (NEW failures: none, DISAPPEARED failures: none)", plus "142 passed, 2 skipped" for the five new tests/test_*_v3.py files. Nothing was left unverified.

## Per-module summary

| Module | Raw findings | Accepted | Rejected | Verdict | Bugs fixed / Perf / Modernized |
|---|---|---|---|---|---|
| `base_loop` | 108 | 20 | 6 | approved-after-hardening | 8 / 3 / 2 |
| `order_utils` | 85 | 15 | 10 | approved-after-hardening | 6 / 4 / 3 |
| `execution_policy` | 53 | 10 | 6 | approved-after-hardening | 1 / 0 / 1 |
| `risk_budget` | 76 | 13 | 7 | approved-after-hardening | 3 / 0 / 0 |
| `bet_sizing` | 62 | 9 | 7 | approved | 7 / 1 / 1 |
| **Total** | **384** | **67** | **36** | — | **25 / 8 / 7** |

## Shipped

- **`base_loop`** — `fix: base_loop 2026-07 panel — flatten tracking contract (P0), breach-state prune, stop-exit guards + journal rows, silent-failure visibility, hot-reload backoff, state-write dedup`
- **`order_utils`** — `fix: order_utils panel v3 — keep-best maker fill evidence, NaN quote guard, bidirectional symbol variants, stream pacing, bounded cancel waits, two-phase flatten`
- **`execution_policy`** — `feat: execution_policy hardening — degraded-input reason vocabulary, name_class echo, fail-closed non-finite/non-numeric guards, 33-test characterization pin + DECLARED-AHEAD dormancy marker`
- **`risk_budget`** — `fix: risk_budget GATE-1 honesty (rho bracket, missing/skewed/ages, NaN-ts guard) + bounded-flock/fsync write path + simulator guards; 28 tests`
- **`bet_sizing`** — `fix: bet_sizing fail-closed input perimeter (knobs/odds/base_rate/step/concurrency) + ndtr swap + NOT-WIRED units docstrings + 17 pin tests`

## OWNER DECISIONS (deferred — model-facing or policy-semantic)

Every `deferred_to_owner` line from the panel, verbatim, grouped by module in panel order. These are not shipped fixes — they are decisions only the owner can make (model-facing thresholds, wiring choices, or policy semantics). Reviewer-consensus notations (`5/5`, `verified:`, `Rn`, seed labels) are preserved exactly as the panel wrote them.

### `base_loop` (22 items)

- [5/5] LLM outage cadence cluster: _last_llm_time advances only on success so a provider outage collapses the 600s cadence to a 30s retry storm (full candidate rebuild + provider call per cycle, 20x spend, stop-management latency); _check_llm_staleness is structurally unreachable (only called within 600s of a SUCCESS, when age is bounded ~10min) and its forced-refresh side effect (_last_llm_time=0 at :1003) makes the storm permanent; _print_startup preloads llm_analysis.json into llm_scores with no enabled/age check, so a DISABLED LLM keeps vetoing entries and tilting size from an arbitrarily old disk cache. Sketch: stamp self._last_llm_attempt unconditionally before analyze_trades and gate on max(attempt, success) with doubling backoff (shadow.py:122 'set first: a broken challenger must not retry every 30s cycle' is the in-repo precedent); run the staleness check from the top of _run_llm_analysis judging in-memory state; gate the startup preload on load_llm_config().get('enabled') plus a 2h age cap.
- [5/5] LLM veto strikes are not scoped to CONSECUTIVE analyses: symbols absent from new_scores (routine — stock scores only top-N) keep frozen strike counts, so one injected score days later forces a liquidation, defeating the documented arXiv 2601.13082 defense (:939-948, :1143). Sketch: after the strike loop, pop any symbol not in new_scores (one line; strictly fewer liquidations; also bounds the dict and the GUI 'veto' badge staleness). Changes when forced liquidation fires — owner sign-off required.
- [5/5, seed a] Trailing-level resting-stop fills are journaled and locked out as hard stops (:699-720, KNOWN comment at :713-717): a profitable trailing exit gets the 24h lockout and journals conflate winners with stop-outs. Sketch: compare the filled order's stop_price (or crypto_loop._resting_stop_px[symbol]) to the entry-anchored hard level pos.entry_price*(1-stop_dist); tag exit_reason 'server_stop_hard' vs 'server_stop_trail'; apply the lockout only on hard. Cross-file: crypto_loop.
- [5/5, seed b] Add-on buys construct a fresh Position without stop_order_id/trailing_activated (:1932-1946), so the live resting stop is orphaned and the full-qty replacement is rejected ('insufficient qty'). Severity sharpened by the panel: it SELF-HEALS in <=1 cycle via _maybe_update_resting_stop's cancel-and-replace branch (stale _resting_stop_px is non-None), so the cost is ~30s of partial server-side coverage plus a spurious ERROR per add-on — but the orphaned old stop is invisible to the server-fill detector (gated on pos.stop_order_id), which can desync qty if it fills inside the window. Sketch: carry prev.stop_order_id/trailing_activated onto the new Position and cancel-then-replace in _after_entry_protection (crypto_loop change needed).
- [5/5, seed c] hard_stop_lockout.json is shared/unprefixed and each book's save wholesale-clobbers the other's lockouts; PLUS (R2, new) the on-disk value is an absolute expiry encoded with the WRITER's HARD_STOP_LOCKOUT_HOURS and decoded with the READER's (:1556-1559 vs :1574-1576) — exact today only because both books use 24h. Sketch: per-book prefix mirroring _position_state_file (needs a one-time legacy-file migration read), or store the START timestamp and read-merge-write.
- [5/5, seed d] /flatten is consumed by whichever book cycles first and never reaches the stock book off-hours (market-closed returns at :213-217 before the check at :223; docstring's own KNOWN LIMITATION at :1621-1625). Sketch: per-book flag files (flatten_request_{asset_type}) written by notify.request_flatten, plus move _check_flatten_request above the market-hours early return (confirm emergency_flatten is safe while the market is closed). Mitigation note: set_halt() already blocks entries in both books — only the liquidation is half-done.
- [4/5, seed + R3/R5] Partial-fill exit handling: status=='partially_filled' falls through to a full-exit record + pop (:838-848), orphaning the unsold remainder with its protective orders already cancelled; conversely a canceled-with-partial-fill keeps the full stale qty, and tracked qty is NEVER reconciled downward (the 'position EXISTS but shares unavailable' branch at :822-824 discards the verify_position object it already holds, wedging the exit forever); the desync branch also drops tracking on a single transient verify_position None. Sketch: on any non-clean fill refresh qty from verify_position, journal only the sold portion (manage_order_lifecycle's docstring already says judge by filled_qty), re-run _after_entry_protection for the remainder; reconcile pos.qty downward in the insufficient-qty branch; require 2 consecutive verify_position None (mirroring _pending_breach discipline) before dropping tracking.
- [3/5] Peak-equity restore is fed the PEAK_SEED placeholder, not real equity: _update_equity first runs at cycle 1, AFTER _reconstruct_positions calls restore_peak_equity(saved, self._equity==100_000) (:100-102, :182-188, :344-351, drawdown max(sp,cur)) — on any sub-$100k account the peak pins at 100k and the drawdown ladder clamps every entry at 0.25x forever, invisible on the default paper account. PLUS (R4) the cycle%10==1 refresh is phase-locked to a counter that ticks during market-closed cycles, so the stock book can trade ~4.5min after the open on seed equity with no macro regime. Sketch: fetch equity once in run() before _reconstruct_positions; replace the modulo trigger with freshness-based refresh (first open cycle always refreshes). Sizing-facing — quantify against the dd_mult journal field before shipping.
- [2/5] Prediction fan-out liveness: no timeout on as_completed (:877) and no timeout on the REST client (trading_utils.get_api), so one half-open socket wedges the entire live loop (stops, sells, kill switch all stop); five wedged workers permanently brick the persistent 5-worker pool; the pool is never shutdown(). Sketch: as_completed(futures, timeout=4*LOOP_INTERVAL) with partial-results fallback (missing preds already fail closed via no_pred), request timeout in get_api (trading_utils, cross-file), atexit shutdown(cancel_futures=True).
- [4/5] Startup sequence sits outside the crash firewall: _load_models catches only FileNotFoundError (corrupt .pth/.pkl → RuntimeError/UnpicklingError propagates), cancel_all_open_orders runs BEFORE reconstruction (a crash between them leaves protective legs stripped, and run_pipeline restart loops re-strip them), and under run_bots one book's startup exception sets the shared _shutdown, killing BOTH books with exit code 0. Sketch: broaden _load_models' except (model=None already fails buys closed via no_pred), bounded retry on the network-dependent startup steps, per-phase guards that preserve fail-closed semantics (_buys_allowed=False on breaker-phase failure), and consider reordering cancel_all_open_orders after reconstruction.
- [3/5] Macro-regime / corr-matrix staleness TTL: a frozen last-successful regime (no TTL) or 3-day-old corr matrix defeats the DEGRADED-MODE clamp, which tests `vix is None` / `not self.corr_matrix` — both False for stale-but-present objects (:1473-1478). Sketch: expire self.macro_regime (set None) after ~30min of failed refreshes so every consumer falls back to neutral AND the clamp arms; same shape for corr_matrix. Model-facing (changes sizing/stops in degraded states). The observability half (timestamps + warnings) ships in this batch.
- [2/5] _pending_breach confirmation is consumed BEFORE _execute_stop_exit is known to have acted: its three early-return retry paths discard the confirmation, so a blocked exit re-arms from scratch and retries at 60s granularity instead of 30s in exactly the fast-market case. Sketch: _execute_stop_exit returns bool; on False restore _pending_breach[symbol]=stop_reason. Exit-timing change.
- [4/5] Now that the provably-dead 1.5x post-lockout threshold bump is deleted, decide whether a REAL graduated re-entry bar is wanted: needs a separate _recent_hard_stop dict with its own decay window that _is_hard_stop_locked does not consume; entry-threshold change → challenger→shadow.
- [1/5] sentiment_gate can never veto (clamp floor 0.15, verified sentiment.py:1220): decide between deleting the defensive gate<=0 branch outright (this batch only corrected the comment and kept it) or introducing a real catastrophic-floor sentiment veto — the latter is a NEW entry veto, challenger→shadow. Cross-file note: decision_report.GATE_REASONS will forever report n=0 for sentiment_block either way.
- [1/5] LLM veto liquidation stays fully armed while self.model is None: _run_llm_analysis and _execute_llm_veto_sells run unconditionally (:263-270), so a model-less bot can market-sell positions on LLM judgment alone while the same missing model blocks every entry. Sketch: skip both when model is None (matching the fail-closed entry stance), or tag llm_analysis rows model_loaded=False so llm_eval can exclude them.
- [1/5] _book_stop_risks fails OPEN: a raising _desired_stop_for contributes 0.0 (:688-689) and a NaN entry_price flows through max() silently — the MAX_BOOK_RISK_PCT cap under-binds with nothing logged, in a repo whose rule is fail-closed in live risk paths. Sketch: append a conservative bound (entry_price*qty*ATR_STOP_CEIL_PCT/equity) on exception/non-finite, log once per symbol.
- [1/5] Positions in symbols that left the universe are permanently orphaned at startup (reconstruct/flatten/cancel all universe-scoped): a startup orphan detector is wanted, BUT the adjudicator notes R2's sketch is incomplete — both books share one Alpaca account, so a naive not-in-universe check false-positives on the sibling book's entire holdings; the detector must filter on the position's asset_class ('crypto' vs 'us_equity'), and any adoption-as-exit-only policy needs a per-book claim rule.
- [2/5] entry_window rows are never emitted when entries are blocked (buys_allowed is a constant True in every emitted row; halted/stand-down/breaker windows are invisible to Stage-0, biasing the admitted-k distribution toward calm regimes). Sketch: emit blocked rows with a blocked_by reason — requires relaxing the n_candidates<=0 guard PINNED by tests/test_conviction_journal.py::test_window_skipped_when_no_candidates and a decision_report change (file under the panel-batch-0 campaign); ship all three together.
- [1/5] Position.trailing_activated is persisted and rendered by the GUI but never written at runtime by base_loop — always False for every crypto position opened since the last restart (writes exist only in stock_loop). The naive write-back is UNSAFE: stock_loop._manage_stops reads the flag to drive server-side trailing placement before calling super(). Sketch: scope the write-back to asset_type=='crypto', or persist the freshly computed trailing_active under a separate key at :311.
- [3/5] place_buy_order is an @abstractmethod the base never calls (entry path is _execute_entry_order → place_limit_order), forcing crypto_loop's dead delegation stub. Sketch: demote to a stock-only hook + delete the crypto stub — but tests/test_review_b01.py pins the stub, crypto_loop.py is under the GUI campaign, and the prior ledger explicitly chose the delegation option; land all three together or not at all.
- [1/5] compute_kelly_fraction re-opens and re-parses the whole (monotonically growing) trade_memory.json once per surviving candidate per cycle inside _compute_position_size. Sketch: mtime-keyed memoization in trading_utils (cross-file — the repo's standard cache idiom), or hoist kelly_f per-cycle; note _compute_position_size's formula/keys are byte-pinned.
- [1/5] Crypto entry priority is fixed market-cap universe order while the book-risk budget demonstrably binds — a 3x-threshold LINK signal is structurally outranked by a 1x BTC signal. KILL-LIST GUARD: 'Shipping concentration/sizing changes on literature priors alone — must measure in-house first' applies directly; the entry_rank annotation shipped in this batch is the sanctioned Stage-0 measurement step, and any ordering change waits on those journals.

### `order_utils` (9 items)

- Lifecycle unknown-outcome return contract (R1/R3/R5; consensus 3): the give-up-after-3-errors branch (order_utils.py:377-385) and the fallback-submit-failure handler (:475-477) return None while fills were observed (saved_filled/final_order discarded). Sketch: cache the last successfully fetched order object and return it (and return final_order on fallback-submit failure); requires revisiting the b02-pinned philosophy in tests/test_review_b02.py::test_none_when_post_cancel_fetch_fails and the docstring's 'or the market fallback failed to submit' clause — decide together with the maker-ladder None-handling below. Instrumentation shipped now makes the event frequency measurable.
- place_maker_buy treats lifecycle None as zero-fill and re-sends the FULL remaining notional on the next rung and again in the fallback — up to 3x intended exposure past RISK_PCT_PER_TRADE/MAX_BOOK_RISK_PCT (R2/R3/R4/R5; consensus 4). Sketch: on result None, abort the ladder AND skip the taker fallback (return (last, 'unfilled_unknown')), optionally reconcile once via verify_position; pair with deterministic per-rung client_order_id (tag+hash of symbol|cycle|attempt) so a broker-accepted-but-client-timed-out submit collides as a duplicate instead of stacking a second live GTC bid.
- emergency_flatten (and the loops' stop-exit paths) confirm market liquidation orders with manage_order_lifecycle, which CANCELS the order at its 15s timeout — a slow flatten is withdrawn with its protective legs already canceled (R1/R2/R3/R4; consensus 4). Sketch: add cancel_on_timeout=True to manage_order_lifecycle (default byte-identical) and pass False from emergency_flatten, or add a confirm-only _await_terminal poller; base_loop:827/crypto_loop:133 belong to the loops campaign.
- check_circuit_breaker semantics bundle (R1/R2/R3/R5): (a) trigger is ACCOUNT-wide while each book flattens/halts only itself — one book's drawdown liquidates the healthy other book; sketch: optional book-scoped baseline (own positions MV + realized PnL vs own start-of-day) with the account reading kept as a larger-threshold kill switch. (b) last_equity<=0 returns a fabricated (False, 0.0) instead of the documented fail-closed (False, None) — one-line change but it suspends live buys on a degenerate account state. (c) crypto weekend baseline: last_equity is the prior EQUITY-day close, so the 'daily' 5% stop measures Sat-Mon drift and the 16:05 ET reset cannot roll over a weekend. Docstring caveats + degenerate-baseline warning shipped now.
- compute_limit_price wide-spread branch (offset = 20% of half-spread) is arithmetically never marketable above a 0.1% spread and steps DOWN in aggressiveness at the boundary (5bps -> 1bps at spread_pct=0.1), so every 'marketable fallback' above 0.1% is a ~30s passive wait then an UNCAPPED market order, and wide-spread stock entries with fallback disabled simply never fill except by adverse selection (R2/R3/R4/R5; consensus 4). Resolve together with the already-open IOC-cap live wiring item, the execution_policy.py divergent tactic table, place_stock_limit_order's unconditional round(,2), and _round_price_band's crypto 2dp branch — one execution-pricing decision, needs paper A/B.
- Maker-share fidelity is MODEL-FACING (R1/R2/R5; consensus 3): entry_tactic is all-or-nothing while fills are mixed ('taker_fallback' after a 95%-maker entry; 'maker' for an instantly-crossed bid-join), and fees.realized_crypto_maker_share counts startswith('maker') to blend the LIVE entry fee in should_trade(live=True) — the label error moves the live cost floor. Sketch: journal maker/taker notional split per entry and switch fees.py to a notional-weighted share; requires Jetson re-measurement of the realized share. Shipped now: one INFO log of posted bid vs fill price at maker returns.
- Crossed-quote rejection in get_quote (R3): extending the degenerate guard to ask<bid is a live entry+stop-path gate change (returning None skips a stop-check cycle). Measure first with the CROSSED warning shipped in this batch, then decide.
- _is_not_found tightening (R4): bare '404'/'not found' substrings misclassify transient errors (e.g. 'request-id abc404def'), suppressing the only warning before a live position is dropped from tracking — but over-tightening risks warning spam on every routine variant 404 probe. Fold into the OPEN P1 absence-vs-failure decision-queue item (verify_position/reconstruct_positions) rather than fixing the classifier in isolation; sketch: prefer exc.status_code==404, regex \b404\b + 'position' keyword, keep phrase signatures.
- should_trade/direction gate-ordering divergence (R2): base_loop calls should_trade BEFORE its pred>threshold check, stock_loop after, so the two books' cost_floor skip buckets measure different populations in decision_report. Caller-side fix belongs to the loops campaign; the direction-blind docstring note ships now.

### `execution_policy` (8 items)

- Q1 — edge_floor source pick (the ONE decision blocking any wiring): choose fees.round_trip_cost_pct (raw cost; then EXEC_EDGE_HEADROOM_MULT must be raised above MIN_EDGE_MULTIPLE=2.0 or the headroom branch is vacuous for every live-admitted trade) vs fees.required_edge_pct at the SAME live spread (effective bar 3.0x raw cost; matches backtest.py:222's variable binding — but note backtest feeds the FLAT SPREAD_PCT while live would feed the per-quote spread, an asymmetry that also needs a ruling). Sketch: rename the parameter to the chosen quantity (or drop it and compute internally via fees), set EXEC_EDGE_HEADROOM_MULT relative to it consciously, update the UNPINNED-CONTRACT docstring paragraph and TestEdgeFloorContractFork in the same change.
- Q2 — name_class production semantics: build the Eff_Spread_Pct seed table + weekly refresh emitting exactly {'mega','mid','spec'} (also consumed by IOC_CAP_BPS); decide whether unknown/case-variant classes should raise or warn instead of silently defaulting 'mid', and whether any SECTOR_BUCKETS->liquidity-class mapping is wanted at all. Changing what 'MEGA'/'spec_growth' resolve to changes the chosen tactic — model-facing.
- Q3 — unknown asset_type routing: keep warn-and-use-stock-table (shipped), vs normalize (str.strip().lower()), vs fail closed to cross/0.0. Any change alters the tactic a mislabelled caller receives — model-facing.
- Q4 — spread sanity ceiling: a finite-junk quote (bid 0.01/ask 1.00 passes get_quote's degeneracy checks) reaches the table as spread_pct~196 and yields post_offset_pct up to 100.0 (now pinned by test_offset_is_unbounded_by_design_today). Sketch: EXEC_MAX_SANE_SPREAD_PCT (~5.0) routing above-ceiling spreads to the missing-data cross path with reason 'spread_implausible_cross' — changes output for finite inputs, model-facing.
- Q5 — ladder/post executability for stocks: no stock-side passive primitive exists (live stock entries are atomic bracket parents; re-chasing the parent limit drops the stop/TP children), and the table carries no rung count/stage timeout. Sketch: wave-7 bracket-parent redesign first, then decide whether EXEC_LADDER_* schedule constants belong in strategy_config and in the returned dict. Note: the offline validator for 'post' economics (honest-OHLC passive-fill simulator) is on KILL_LIST.md — validation needs live Jetson maker journals.
- Q6 — whether choose_entry_tactic should itself read MAKER_ENTRIES_ENABLED / ENTRY_WINDOWS_ENABLED and downgrade post/ladder->cross when off (currently a documented caller obligation; making the function config-dependent changes its output for a given input and its purity).
- Q7 — tick-grid awareness: below roughly price*spread_pct=2.5 the emitted percent offset is smaller than the equity penny tick, so post/ladder legitimately degenerate to a touch-join on exactly the cheap wide-spread names they target (now documented in the Returns docstring). Sketch: return a price-space offset or a min_offset_ticks notion — changes emitted entry prices, model-facing.
- Q8 — edge_floor<=0 semantics: now documented and pinned as 'cost floor unavailable -> disable post' (fail closed); flipping to 'zero cost = infinite headroom' would change the tactic for those inputs — model-facing. (The abs(pred_return) short-headroom item remains deferred to the short-wiring work per fixes_deferred[6]; not re-opened.)

### `risk_budget` (9 items)

- Simulator compounded-equity mode (5/5 reviewers flag the 'compound into the SAME equity curve' docstring vs additive np.cumsum): switching to np.cumprod(1+pnl/100) with weights against running equity changes every GATE-2 number (combined_max_drawdown_pct, sharpe, concentration) — i.e. promotion evidence. Sketch: add optional compound=True emitting combined_max_drawdown_pct_compounded ALONGSIDE the additive baseline so the gap is measurable before anything switches; two books each -30% same period = 51% compounded vs 60% additive. Doc-only half ships in this batch.
- simulate_two_books empty-path schema unification (return all 9 keys with 0.0/None, honour explicit `periods` in n_periods): requires widening the exact-dict assertion in tests/test_review_b12.py::test_simulator_no_trades_schema_and_sharpe_doc — a shape extension, not a weakening, but it edits an existing test. The reduced 5-key shape is now documented in the docstring.
- Registry write cadence + staleness reconciliation: base_loop writes every 10th cycle (~300-700s, doubling under thermal throttle) vs gui.py's 600s grey-out and this module's 900s — under 2x margin, and stale-drop rate correlates with cycle load (highest exactly when risk is highest). Sketch: move _record_account_risk out of the %10 gate (it is ~1ms journal-only) or require consumer windows >= 3x cadence; belongs to base_loop's own panel in this same batch A. Adjacent to (not the same as) the already-recorded overnight/weekend stale-drop P2, which remains open and is now measurable post-hoc via book_ages_s/missing_books.
- base_loop honesty wiring for the new book_risks=None capability: _book_stop_risks returns [] for BOTH 'flat' and 'equity unknown' (base_loop.py:680), and _update_equity's bare `except: pass` can pin _equity at PEAK_SEED=100000 making every written risk fraction silently wrong-scaled. Sketch: return None when self._equity <= 0 or equity is the untouched seed; risk_budget now skips the write on None so the entry honestly ages into stale_books. Changes the journaled evidence stream — base_loop panel + owner sign-off.
- gui.py (under GUI-campaign owner review): import ACCOUNT_RISK_REGISTRY and REGISTRY_STALE_AFTER_S from risk_budget instead of the independent BASE_DIR literal (gui.py:84) and hardcoded 600s (gui.py:3579); apply the same two-sided ts-freshness check. risk_budget's path anchor (shipping now) already makes the two agree by construction under all launches.
- Add `account_risk_registry.json.*` to .gitignore (covers the .lock sidecar and .<pid>.<tid>.tmp staging files, currently untracked noise on the Jetson): .gitignore is dirty under the GUI campaign — one-line owner edit.
- Offline GATE-1 journal aggregator (R3): nothing in the repo reads the ~290 account_risk rows/day — no computed answer to 'what fraction of cycles were over_cap / max book_sum / stale frequency by hour'. Sketch: ~40-line standalone CLI in decision_report.py style taking rows as an argument (Mac-testable on synthetic rows); standalone because decision_report.py/llm_eval.py are under Panel-batch-0 review.
- Relocate ACCOUNT_RISK_CAP into strategy_config.py next to MAX_BOOK_RISK_PCT/CROSS_BOOK_RHO (no import cycle — portfolio imports strategy_config lazily): touches the declared policy single-source-of-truth file. The false 're-export' comment is corrected in this batch either way.
- Replace CROSS_BOOK_RHO=1.0 placeholder with a realized cross-book correlation feeding the report/clamp: model-facing wiring reserved for the challenger->shadow->DM-HLN promotion path per risk_budget.py:237's own comment. The account_stop_risk_indep bracket shipping now makes the journal informative in the meantime.

### `bet_sizing` (6 items)

- FLAT-TOP / CAP-SPACE (5/5, verified: kelly_edge_odds(p,0.04,0.02,cap=KELLY_CAP=0.25) returns exactly 0.2500 for every p in [0.35,0.90]; saturation onset p=0.3367 vs breakeven 0.3333): f* is NOTIONAL leverage (O(1/a)) while KELLY_CAP is denominated as a risk fraction, so the wiring strategy_config.py:169-175 prescribes yields a CONSTANT — flatter than the clip(2p,0.6,1.3) it replaces, and Stage-0 would measure a false null. Sketch: choose the cap space before any wiring — (A) cap the risk quantity f*·a against the RISK_PCT_PER_TRADE scale, (B) use f*(p)/f*(p_ref) as a bounded conviction multiplier in the existing [0.5,1.5]-style band, or (C) demote kelly_edge_odds to a breakeven gate and make afml_bet_size (naturally graded in [-1,1]) the sizer; correct the strategy_config comment in the same change; expose pre-cap f*/cap-bound in whatever form is chosen so decision_report.py Stage-0 can attribute; then run the Stage-0 rank-gradient measurement per KILL_LIST's 'must measure in-house first'.
- afml_bet_size DEFAULT base_rate=0.5 vs economic breakeven 1/3 under tp_rr=2.0 (2/5, verified: afml_bet_size(0.45)=-0.080 while raw Kelly at the same p is +8.75, so a long-only max(0,size) caller zeroes every +EV trade with p in (1/3,1/2)): decide whether base_rate becomes required (breaks tests/test_bet_sizing.py:61's default-arg call), defaults to None meaning caller must pass breakeven_p(b,a), or stays 0.5 as the AFML classifier null with the economic gate applied upstream. Shipped docs state the trap; the default itself is a threshold choice.
- VIX DOUBLE-COUNT in live sizing (5/5, verified from source: base_loop.py:1389-1394 f_vix ladder {0.3/0.5/0.7 at 35/25/15} and macro_indicators.py's sizing_mult {0.3/0.5/0.8 at the SAME breakpoints, same fetch_vix reading} both multiply into tilt -> 0.09 at VIX>35, BELOW the 0.1 floor at base_loop.py:1464, making every other advisory multiplier inert exactly in crisis; a third VIX read caps kelly_mult at vix>25 at base_loop.py:1436-1437, and the disagreement vote thresholds the VIX-driven sizing_mult at ~1422): measure the co-fire rate + P(tilt_raw<=0.1) from the already-shipped sizing-decomposition journal rows, then keep exactly ONE VIX tier ladder (recommended: strip the VIX block from macro_indicators so sizing_mult carries only stress/CAPE/stablecoin, keeping base_loop's f_vix as the single authority — or the reverse), decide separately whether the kelly_mult clamp is a third count or a deliberate procyclicality brake, re-gate via backtest.py --gate, challenger->shadow path. NOTE for the edge-Kelly wiring: the binding stress constraint is the 0.1 FLOOR, not TILT_MAX as strategy_config.py:174 records.
- kelly_edge_odds INPUT CONVENTION vs this system's exit stack (1/5, R1, arithmetically verified): the docstring's gross TP/stop barrier inputs paired with meta_label's cost-net P(net>0) misstate the two-outcome bet — plausible conditional means from the 5-way exit stack (win 1.2%/loss 1.8%) flip f* from +12.5 to -13.9 at the same p=0.5, and the gross breakeven 0.3333 vs cost-aware 0.3792 leaves a band where the kernel sizes positive on negative net EV. Sketch: decide the input convention (cost-net conditional mean win/loss from journals vs gross barriers plus an explicit cost=0.0 kwarg wired to fees.round_trip_cost_pct) before EDGE_KELLY_ENABLED wiring; adding cost=0.0 defaulted is behavior-neutral, any nonzero use is model-facing.
- concurrency_scale FATE (5/5 on dead-code-plus-trap): zero non-test callers, not named in the strategy_config wiring plan; 1/K is the perfect-correlation bound and would stack on the ENB book cap + correlation factor (same class of error as the VIX double-count); entry-time 1/K also != AFML's average-of-active-bets (sequential fills hold 2.08x the average form at K=4); int() truncation oversizes fractional n. Decide: delete it (plus its one test) or keep it fenced by the shipped docstring; if ever wired it must REPLACE one of the existing concurrency de-risks; the int->float truncation change is a sizing-value change requiring the model-facing path.
- breakeven_p SENTINEL (1/5, low confidence): 1.0 is an attainable probability (kelly_edge_odds clips p>1 to exactly 1.0), so a >= gate trades on degenerate odds; changing the sentinel to inf would edit five pinned b13 assertions. Shipped: docstring requires strict >. Owner may later decide the inf contract is worth the pinned-test churn.

## Two findings that need attention before any wiring

Both surfaced in the `bet_sizing` panel (full reviewer notation in OWNER DECISIONS → `bet_sizing` above); foregrounded here because either one would silently invalidate a Stage-0 measurement if wired as currently specified.

**(a) bet_sizing flat-top / cap-space.** `kelly_edge_odds` returns exactly 0.2500 for every `p` in [0.35, 0.90] because f* is notional leverage while `KELLY_CAP` is a risk fraction — wiring it the way `strategy_config.py:169-175` prescribes yields a CONSTANT, flatter than the `clip(2p, 0.6, 1.3)` it replaces, and Stage-0 would measure a false null.

**(b) VIX triple-read in live sizing.** `base_loop.py:1389-1394`'s `f_vix` ladder AND `macro_indicators`' `sizing_mult` fire on the SAME breakpoints and the SAME reading, and both multiply into `tilt` -> 0.09 at VIX>35 — BELOW the 0.1 floor at `base_loop.py:1464`, making every other advisory multiplier inert exactly in a crisis. A third read caps `kelly_mult` above VIX 25.

## Verification

- **Regression gate (run manually — the workflow's own `[gate]` agent failed):** "ab_check: PASS — zero regressions vs tests/baseline_failures.txt (NEW failures: none, DISAPPEARED failures: none)"
- **New tests for this batch:** "142 passed, 2 skipped" across the five new `tests/test_*_v3.py` files (one per module).
- **Nothing is committed.** Every `commit_line` above is a proposal for the owner's review — the owner commits after review, same as every other campaign in this repo.
- **Bounded by this dev Mac's installed deps.** No torch/lightgbm/optuna/joblib/numba/sklearn/dotenv here; verification covers the pure-python-provable parts and the full suite's pass/fail test **names**, not a live Jetson end-to-end run.

---

*Generated by module-improve-v3 batch A (Opus panel -> Fable spec -> Sonnet implement -> Fable harden); report reconstructed after the workflow's report agent hit a session limit.*
