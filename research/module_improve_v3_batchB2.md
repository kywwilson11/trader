# Module improvement campaign — panel review (v3) — Batch B2: Promotion Gate (meta-label / policy replay / exit kernel)

## Verdict

Panel-review batch B2 covered the three modules that make up the promotion gate — `meta_label`, `policy_exits`, and `backtest` — each reviewed independently by 5 Opus reviewers against an identical brief, adjudicated into a single hardening spec, implemented by Sonnet, and hardened by Fable. Combined: 224 raw findings (82 / 64 / 78) spanning the meta-labeling gate, the shared exit-labeling kernel, and the policy-replay backtester. All three landed as **approved-after-hardening**: everything shipped is a fail-loud guard, torn-artifact/race hardening, provenance/instrumentation, doc-truth correction, a byte-budgeted memory bound, or a narrowly-scoped bugfix on a latent/unreachable path — zero changes to feature values, thresholds, entry/exit/sizing semantics, label construction, or model artifacts shipped from this pipeline. No modules were dropped for budget. The regression gate (`bash scripts/ab_check.sh`) passed clean, run twice for stability: zero NEW failure names and zero DISAPPEARED names against `tests/baseline_failures.txt`, exit code 0. 36 findings were deferred to the owner as model-facing or policy-semantic decisions, including two P0 cross-file items in `backtest` that touch the DSR promotion gate's mathematical soundness and the shadow-mode gate's wiring.

## Per-module results

| Module | Raw findings | Accepted | Rejected | Verdict | Bugs fixed / Perf / Modernized |
|---|---|---|---|---|---|
| meta_label | 82 | 13 | 7 | approved-after-hardening | 8 / 4 / 3 |
| policy_exits | 64 | 7 | 5 | approved-after-hardening | 2 / 0 / 0 |
| backtest | 78 | 15 | 8 | approved-after-hardening | 3 / 1 / 1 |

## Shipped

- fix: meta_label — NaN-p tilt guard, torn-pair load retry, fail-loud backtest scoring, provenance meta_meta.json + Jetson memory hygiene (panel v3, 14 tests)
- docs: policy_exits — normative kernel precedence/caveat contracts + 63-test exact-value pin suite (batch B2)
- feat: backtest.py 2026-07 hardening — DSR/n_eff provenance, gate-verdict persistence, manifest-last rollback + crash banners, byte-budgeted inference chunks

## OWNER DECISIONS (deferred — model-facing or policy-semantic)

Every line below is verbatim from the panel's deferred_to_owner output, grouped by module in panel order; these are decisions only the owner can make, not shipped fixes.

### meta_label (11 items)

- MODEL-FACING — Replay entry-policy parity: _gen_meta_rows omits the cost/edge floor (live admission ~ required_edge_pct: order-of-magnitude gap for crypto vs 0.5x threshold), the 24-bar post-hard-stop lockout (backtest.py:281-285 'cooldown_after = max(cooldown_bars, lockout_bars)'), the stock entry-window mask (backtest.py:257 'not entry_ok[i]', ENTRY_WINDOWS_ENABLED=True), and the q10 tail veto — training the veto on trades live can never take. Sketch: default-OFF flag (e.g. META_REPLAY_POLICY_PARITY); unpack exit_walk's reason (currently '_reason'), apply backtest's lockout rule verbatim; share _entry_window_mask (move into policy_exits next to eod_mask_from_index); decide the edge-floor question explicitly (R4 argues the floor relaxation may be intentional with the threshold lowering; R1 measured the gap). A/B base_win_rate/n_trades/holdout Brier before flipping; add a source-regex parity test in the test_cooldown_derivation_in_sync style. Sequence AFTER the calibration package so effects don't confound. (5/5 reviewers)
- MODEL-FACING — rev-07-01 in-sample-primary leak CONFIRMED still open: _load_artifacts loads the deployed primary and scores exactly the pre-cutoff window it trained on, so the 'pred' feature AND the entry filter selecting meta rows are in-sample; calibration.py's docstring states no fold arrangement there can fix it. Sketch (Jetson-affordable): have hypersearch_v2 persist its per-fold validation predictions as {p}primary_oof keyed (Ticker, timestamp); train_meta joins and uses them for both the pred feature and the line-288 filter, falling back to today's path with a loud '[META] pred is IN-SAMPLE' warning + pred_source stamp in meta_meta.json. Reject k-fold LSTM refits inside meta_label (8 GB Jetson). (5/5)
- MODEL-FACING — Calibration publish gating: the DEFAULT 'legacy' branch (line 400-402) has none of fit_calibrator's degenerate guards (a one-class 20% calibration tail publishes a constant p=0.0 that vetoes the whole book, or constant ~1.0 that boosts everything), fit_calibrator's own decline falls INTO that unguarded branch, no skill floor exists (val_auc recorded, never tested), and shadow.py's post-promotion background retrain reaches live with NO gate. Sketch: route legacy through fit_calibrator's guard semantics; refuse to publish (return False, leaving the previous pair = documented neutral) on degenerate calibrator or val_auc <= 0.5+margin; give train_meta --no-publish/staging so shadow's retrain can be gated. The shipped p-distribution/frac_below_veto instrumentation makes the failure visible meanwhile. (5/5)
- MODEL-FACING — B1 cross-batch calibration package, one sequenced decision (do NOT edit calibration.py from any meta batch, and do NOT flip META_CALIBRATION_MODE before it lands): (a) B1's IsotonicCalibrator tie-collapse fix FIRST (fit[last] = tie-block max vs sklearn's mean-pooling; materiality contested — R2/R3/R5 'ties are the norm', R4 'rare for 400-tree sums' — but the flip is confounded regardless because it changes SAMPLE (last-20% slice -> all-row OOF), FORM (always-isotonic -> sigmoid below n=1000, crossed at different sizes), and IMPLEMENTATION at once; add a third same-sample arm to compare_calibrations to decompose); (b) nonzero embargo at meta_label.py:392 in ABSOLUTE SECONDS — purged_kfold_indices' dual semantics make a fractional value a trap (embargo=1.0 there = one second); size to the longest trailing feature window (~20-48h*3600); (c) pass n_eff = sample_weights.clustered_effective_n(entry_e, exit_e) into the calibration-method choice (caveat: transitive clustering may collapse n_eff so far isotonic becomes unreachable — measure on the Jetson first); (d) per-fold early stopping to close the full-sample n_iter leak the shipped comment now names. (5/5)
- MODEL-FACING — features_from_snapshot's 'pred' branch lacks the finite guard every other feature has: live NaN/inf pred reaches LightGBM as a missing value (a branch training never exercised, since build_feature_matrix nan_to_nums to 0.0). Sketch: apply the identical guard (finite -> value, else 0.0, TypeError/ValueError -> 0.0) — restores train/serve parity; currently unreachable through base_loop's upstream gates, so zero urgency, but it is a feature-VALUE change on the pathological branch and rides the challenger path. (4/5)
- MODEL-FACING — Deterministic training row order: np.argsort(default quicksort, unstable) over pervasively tied cross-ticker timestamps means fold membership, the 0.8 split, and lgb bagging depend on ticker iteration order; two runs on identical data can publish different artifacts. Sketch: kind='stable' + explicit (timestamp, ticker) tie-break; land together with another artifact-changing meta change to pay one re-certification. The shipped comment marks the line as load-bearing meanwhile. (3/5)
- MODEL-FACING — Holdout boundary unification: meta recomputes the 0.88 row-quantile over the FULL harvest frame while hypersearch computes it over the --max-rows-capped set (stock cap binds: 200k vs ~500k rows). Verified direction TODAY is conservative (capped set skews recent, so hypersearch's boundary is later; crypto uncapped = identical), but nothing enforces it and the docstring claim was false. Sketch: hypersearch persists its resolved boundary (it already writes 'holdout' into the config/manifest); train_meta reads it, falling back to the local quantile with a loud warning. The shipped cutoff recording makes drift observable. (3/5)
- MODEL-FACING — Stale meta<->primary pairing enforcement: every train_meta False path leaves old meta artifacts live against a possibly-new primary (shadow.py deletes them on promotion for exactly this reason; the run_pipeline weekly path has no equivalent), and .prev rotation cadences differ across writers so restore_previous_model can install an (old primary, newer meta) pair. Sketch: on return False, unlink the three artifacts (mirroring shadow) OR have _load/restore refuse a fingerprint mismatch using the primary fingerprint now stamped into meta_meta.json. (3/5)
- ALREADY QUEUED, reaffirmed open: decision-queue #51 (live hour feature uses wall-clock; the skew is a deterministic +1h growing unboundedly with feed staleness — thread the last-closed-bar hour through predict_now's snapshot into features_from_snapshot's existing unused hour param) and #52 (uncaught train_meta crash exits 1 -> run_pipeline retries then cancels the remaining phases including the backtest gate; the shipped publish reorder removes the worst partial-publish variant but the __main__ policy choice remains the owner's). (2/5 each)
- POLICY-SEMANTICS — Load-side feature-schema ENFORCEMENT: the shipped check only WARNS on META_FEATURES drift; whether a mismatch should disable the layer (return None = documented neutral) changes live gate output in the corrupted state. One-line decision once the warning has been observed benign. (2/5)
- POLICY-SEMANTICS / OUT-OF-OWNERSHIP reports: (a) the promotion gate replays the meta VETO but never the size tilt (meta_size_mult's only importer is base_loop) — cheapest honest step is backtest recording meta_probs on each trade dict for offline p-weighted equity (backtest.py edit, not this module's); (b) backtest.py:441-445's bare 'except Exception: pass' should at minimum log (the shipped in-module print compensates); (c) num_threads=1 for the single-row live predict — Jetson-verify the kwarg and value-equality first. (1-2/5 each)

### policy_exits (13 items)

- EOD-BAR ENTRY LABELS (consensus 4, model-facing): compute_tb_labels stamps labels for entries ON the day-last bar — the only stock labels spanning the overnight/weekend gap (~1 row/ticker/day; TB_Bars=7 vs <=6) — while backtest.py:256 and meta_label.py:289 both skip is_eod[i] entries. Sketch: NaN all three TB columns where is_eod[i] (matching the other consumers) or emit a TB_EodEntry flag so training can weight/exclude without losing rows; weigh against OVERNIGHT_SLEEVE_ENABLED; requires re-harvest + Optuna study reset (gotcha #2); decide jointly with the truncation item.
- TRUNCATION MASK NaNs RESOLVED BARRIERS (consensus 2, model-facing): invalid = arange(n)+fb >= n keys off the horizon, not off whether the walk actually touched a barrier inside the data (verified: row with exit resolved at an EOD 4 bars in is still NaN'd). Sketch: resolution-aware mask ((arange(n)+fb >= n) & isin(reason,(0,6))) applied across ALL horizons at once — but note (a) eod_mask's last-bar-always-True means reason 5 at exit_idx==n-1 can mean 'ran out of data', and (b) R2's check that raw Target_Return columns NaN the same tail, so no rows return unless those change too. Re-harvest + study reset.
- TP GAP-FILL ASYMMETRY (consensus 5, model-facing): stop is gap-aware (min(open, stop)) but TP fills exactly at tp_price even when the bar OPENS through it, and an open above the TP with a later same-bar stop breach books hard_stop at 98 where the live stock bracket limit fills at the 106 open (verified, sign-flipped label). Sketch: long e_px = o if o > tp_price else tp_price (mirrored short), plus decide whether open >= tp_price pre-empts the stop check (no intrabar ambiguity there); left tail fully charged / right tail clipped biases DSR down on gap-prone names; NOT the killed wave-7 'Honest-OHLC passive-fill simulator'; re-harvest + study reset; relevant to the open wave-9 'let winners run' idea.
- STOCK TB HORIZON DEGENERACY (consensus 2, model-facing): with ~7 RTH bars/session and fb floor 8, the EOD barrier subsumes every vertical barrier — all five stock TB triples are identical on the finite region and reason 6 is unreachable (verified), so Optuna's forward_bars categorical is a no-op for stock tb-target trials and 10 of 15 stock TB columns are duplicate float64 in RAM on the 8GB Jetson. Sketch: verify RTH bars/day on the Jetson parquet (same check as open item 59), then drop forward_bars from the stock tb search space in hypersearch_v2 or shrink stock horizons below the session; study reset.
- TB_BARS AS POST-FILTER ROW OFFSET (consensus 1, verified, model-facing cross-module): stock harvest stamps TB_Bars (harvest_stock_data.py:136) then drops rows (dropna:154, tradability:155, membership:295), while sample_weights.py:122-126 requires hold_bars to be a consecutive-row offset — interior holes stretch labels across missing rows, overstate overlap, understate effective_n, and feed the promotion gate's DSR (compounds batch B1's clustered_effective_n finding). Sketch: emit TB_ExitTime_{fb} so consumers rebuild spans via searchsorted on the surviving index, or add a consecutiveness tripwire in sample_weights.
- VALIDATION GUARDS (consensus 5, behavior-neutral for every current caller but code changes forbidden this batch): raise ValueError on max_hold<0, cooldown_bars<0, fb<1 in compute_tb_labels, asset_type not in ('crypto','stock') (policy_for defaults to STOCK while eod_mask defaults to crypto — opposite silent directions), ndim!=1, and non-finite/non-positive close/high/low/open (ATR NaN stays legal by design — it is the documented fallback path); wrap policy KeyError into a named ValueError; also NaN TB_Bars/TB_Reason wherever TB_Ret is NaN (close<=0 rows) to restore the fixes_applied[193] all-three-columns invariant. All inside the existing validate-before-dispatch block; preserve tests/test_review_b16.py's pytest.raises(match=...) substrings.
- SILENT NUMBA FALLBACK (consensus 5, instrumentation): one-time warnings.warn in the except ImportError branch ('policy_exits: numba unavailable — pure-python exit walk, O(n^2) worst case') and keep _HAS_NUMBA importable/load-bearing; optionally a run_pipeline Jetson preflight assert. One line; the new test already pins the flag's existence.
- SINGLE-PASS compute_tb_labels (consensus 5, behavior-neutral, bit-identical PROVEN by four reviewers and re-proven here across 6 seeds x 2 policies x 2 sides x 4 horizons): one max_hold=0 walk + O(n) vectorized derivation per horizon replaces len(forward_bars_list) full walks. Honest sizing: negligible under numba, large only in the degraded fallback mode. Gate on TestHorizonDerivability (ships now) asserting exact array equality.
- SHORT-LABEL COLUMN COLLISION (consensus 2, format-contract change): side=-1 emits identical TB_* names, so a merged research frame silently overwrites long labels and hypersearch/wave6_stage0's f-string/regex consumers cannot tell. Sketch: key_suffix parameter defaulting to '' (long-side format byte-identical), short callers pass '_S' which the TB_Bars_(\d+) regex will not match.
- SILENT COLUMN FALLBACKS IN compute_tb_labels (consensus 1, verified): missing ATR routes every entry to fixed-percent fallbacks (R2 measured mean TB_Ret 4.2x shift, all-vertical exits); missing High/Low collapses bars to points. Sketch: raise on missing High/Low/Open/ATR (all production callers supply all five) or warn once naming the fallback taken.
- TRAILING SEMANTICS VS LIVE — MEASURE FIRST (R2/R5, model-facing): three coupled questions — same-bar trail-vs-TP resolution, the one-bar HWM enforcement lag (R5 upper bound 6.4% of entries on synthetic data), and reason-3 exits that live would book as hard_stop (backtest.py:283 applies the 24h lockout only to hard_stop, so the gate replays a more permissive re-entry policy on flash-crash paths). Sketch: Jetson measurement of how often reason==3 exits price at/below entry*(1-sd) on the real parquet before any semantic decision; adjacent to open ledger items 58 and 29; DM-HLN shadow gate is NOT valid validation for any of it (KILL_LIST).
- TB POLICY PROVENANCE STAMP (consensus 1, instrumentation, schema change): label columns carry no fingerprint of the 9 policy floats that generated them, and strategy_config has already been retuned in place (tp_rr 3.0->2.0, stop_floor 0.06->0.015) — pre/post-retune parquets mix silently. Sketch: constant fingerprint column named OUTSIDE the TB_ prefix (test_review_b21 pins TB_* enumeration) + a warn-on-mismatch in hypersearch_v2/wave6_stage0.
- EOD_MASK VECTORIZATION (consensus 3, perf-only, smallest item): replace the per-element .date() object loop with np.asarray(index.date) fast path for real DatetimeIndex, KEEPING the duck-typed comprehension fallback (FakeTS tests) and never values.astype('datetime64[D]') on tz-aware indexes (UTC-vs-local date trap = item 59 territory). The new FakeTS-vs-DatetimeIndex agreement test pins the equivalence in advance.

### backtest (12 items)

- P0 (cross-file, owned by sample_weights.py/batch B1): clustered_effective_n's transitive connected-components collapse makes the gate mathematically unpassable — 3 reviewers independently reproduced ~600-trade books collapsing to 1-21 clusters, requiring per-trade Sharpe 0.44-0.90 (annualized ~21-73) to clear DSR_MIN=0.60. Sketch: replace with a monotone concurrency estimator (n_eff = sum over trades of 1/max-concurrency-during-trade), or floor n_eff at n_trades/max_concurrency in backtest.py, or revert to n_eff=None until B1 lands. Immediate Jetson check (works after this batch even on zero-trade runs): python -c "import json;m=json.load(open('backtest_report.json'))['metrics'];print(m['n_trades'],m['n_eff_clustered'],m['n_eff_source'],m.get('dsr_iid'))" — dsr_iid high with dsr low is the smoking gun.
- P0 (cross-file, owned by run_pipeline.py/shadow.py): under the DEFAULT shadow-mode weekly retrain (run_pipeline.py:947+1501+1657) hypersearch saves to the challenger slot (hypersearch_v2.py:1556-1561) while both *_backtest_gate phases (977/1003) invoke backtest.py on the CHAMPION prefix — the fresh model is never policy-gated (it promotes solely via the DM-HLN test that KILL_LIST.md calls a category error for policy decisions) and a gate failure rolls back the innocent live champion mid-shadow-window. Sketch: in shadow mode either point the gate at challenger_prefix and route rc=3 to 'discard challenger', or call run_backtest on the challenger inside shadow's promotion path. The shipped manifest-provenance warning makes the mismatch visible until then.
- P1 exit-code contract: 'gate failed but NOTHING was restored' still returns 3, which run_pipeline+GUI render as a successful rollback. Sketch: reserve 3 for failed-AND-restored, add exit 4 for failed-no-rollback treated as a real book failure; must land with run_pipeline._run_training in the same change. (The shipped banner + report gate.restored field + critical notify cover the observability half.) Also decide whether a crash (any non-FNF exception) should itself roll back to .prev.
- P1 DSR deflation pool: the gate deflates against this run's --trials (~100) while hypersearch's own holdout gate uses the cumulative resumed-study count (hypersearch_v2.py completed_trials) — after N weekly retrains the policy gate's bar is systematically LOWER than the fit gate's on the same model. Sketch: save_model_atomically records len(completed_trials) into model_v2.manifest.json; backtest.py prefers the manifest value over --trials. The shipped dsr_n_trials field exposes the pool actually used.
- P1 stock hard-stop lockout units: backtest.py:241 consumes lockout_hours as BARS (24 bars ≈ 3.4 RTH sessions) while live base_loop enforces wall-clock hours (24h ≈ 1 session) — the replay validates a stricter re-entry policy than trades. meta_label applies NO lockout at all, so the three paths disagree three ways. Sketch: lockout_bars = ceil(lockout_hours * bars_per_session / 24) per book, resolved for backtest + meta_label together. (Docstring note shipped.)
- P2 tz-naive index handling in _entry_window_mask: whether the naive branch should localize UTC→ET (matching policy_exits' documented convention) or refuse to run — changes which bars are admissible. (Warning shipped.)
- P2 rounded-vs-raw gate comparison: main() gates on round(dsr,4)/round(sharpe,3), so dsr=0.59996 promotes at a documented 0.60 bar. Sketch: compare on the now-shipped dsr_raw/sharpe_raw fields — one-line change, but it changes which models pass (±5e-5).
- P2 entry cost-floor divergence: the replay admits on the flat SPREAD_PCT floor while live's should_trade uses the real quoted spread — permissive for names wider than flat. Sketch: count entries whose own per-bar floor would have rejected them (n_entries_below_own_cost_floor + their summed net_pct) before deciding whether to switch the floor; switching is model-facing. (Comment wording corrected in this batch.)
- P2 ABSTAIN verdict: a replay with 0 evaluated tickers / 0 trades currently produces the same destructive exit-3 rollback as a genuine policy rejection. Sketch: exit 0 + no rollback + 'gate_unmeasurable' outcome when n_tickers_evaluated==0 or coverage below a floor; needs run_pipeline handling in the same change. (Shipped coverage_frac/failed_checks make the states distinguishable in the report.)
- P2 fully-atomic restore: the shipped banner+ordering leaves a partial-OSError restore unrecoverable (.prev already consumed). Sketch: two-phase copy-then-swap (stage .prev copies to *.restore.tmp, os.replace all with manifest last, then delete .prev) so any crash leaves either the old or the new complete set plus intact .prev files.
- P3 corrupt-bar trades: a zero/NaN Close produces an inf/NaN trade that (conservatively) fails the gate; skipping it (entry_price>0 guard) could flip a FAIL to a PASS — owner call. (Counter + warning shipped.)
- P3 in-sample window verification: nothing checks --days stays inside the 12% holdout; R1's local recomputation was NOT shipped because stock hypersearch trains on a --max-rows 200000 TRUNCATED frame, so a boundary recomputed from the full harvest is wrong for the stock book. Sketch: hypersearch persists get_holdout_boundary(all_times) into the manifest at save time; backtest then asserts cutoff >= boundary and warns. (--days help text now documents the 44/60 reasoning.)

## Verification

Gate: **PASS**. Ran `bash scripts/ab_check.sh` in `/Users/kywwilson/Desktop/Projects/trader`, twice for stability. Verbatim: "suite summary: 21 failed, 2495 passed, 18 skipped, 2 warnings, 7 errors in 15.04s" / "NEW failures: none" / "DISAPPEARED failures: none" / "ab_check: PASS — zero regressions vs tests/baseline_failures.txt". Zero regressions vs the committed baseline; no NEW failure names appeared and none disappeared, so no attribution to meta_label, policy_exits, or backtest is needed. Cross-checked `tests/baseline_failures.txt` directly: its 21 FAILED + 7 ERROR entries are all pre-existing missing-dependency failures (dotenv/torch/lightgbm/optuna/joblib/numba/sklearn), matching CLAUDE.md's documented baseline exactly by name. Passed/skipped counts are higher than CLAUDE.md's 2026-07-15 snapshot (2495 vs 1887 passed; 18 vs 15 skipped), but this is expected drift from new test files added by the other uncommitted campaigns (test_command_ack.py, test_design_tokens.py, test_journal_stats.py, test_llm_dossier_persist.py, test_prediction_cache_context.py, test_shadow_status_persist.py, test_tax_lots.py) — ab_check.sh correctly diffs only FAILED/ERROR test names (never counts), which is the project's authoritative regression signal. No files edited, baseline not regenerated, nothing committed.

Nothing in this batch has been committed — all diffs remain in the working tree pending owner review and commit. As with every campaign on this dev Mac, verification is bounded by the two-machine split: any code path in these three modules that depends on torch/lightgbm/optuna/joblib/numba/sklearn/dotenv (meta_label's LightGBM/joblib training and calibration paths; backtest's model-loading and byte-budgeted inference paths) is exercised only via its Mac-side fallback or mock — full-stack confirmation of the shipped hardening still happens on the Jetson.

Generated by module-improve-v3 (Opus panel -> Fable spec -> Sonnet implement -> Fable harden).
