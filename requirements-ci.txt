# CI-only dependencies — CPU torch, no GUI
# Install: pip install -r requirements-ci.txt

# CPU-only PyTorch (installed first via workflow step)
# torch and torchvision installed separately with --index-url

alpaca-trade-api>=3.0.0
python-dotenv>=1.0.0
numpy>=1.26
pandas>=2.0
scikit-learn>=1.5
joblib>=1.3
optuna>=4.0
yfinance>=1.0
numba>=0.60
finnhub-python>=2.4
beautifulsoup4>=4.12
requests>=2.31
PyYAML>=6.0

pytest>=7.0
