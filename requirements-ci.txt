# Jetson-parity CI dependencies — installed by the py3.10 CI leg via
#   pip install -r requirements-ci.txt
# Mirrors requirements-jetson.txt pins so ONE non-Jetson environment runs the
# production numpy<2 / pandas 2.x / lightgbm / alpaca-py stack. CPU-only torch
# is installed by a separate workflow step (--index-url). The py3.12 "modern"
# leg keeps its own unpinned inline install in ci.yml.

alpaca-py>=0.43.0
alpaca-trade-api>=3.0.0
python-dotenv>=1.0.0
numpy>=1.26,<2
pandas>=2.0,<3
scikit-learn>=1.5
joblib>=1.3
optuna>=4.0
yfinance>=1.0
numba>=0.60
finnhub-python>=2.4
beautifulsoup4>=4.12
requests>=2.31
PyYAML>=6.0
bidask>=2.1
lightgbm>=4.0

pytest>=7.0
